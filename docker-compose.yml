services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: allergo-trace-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DB_PATH=/app/data/bot.db
    volumes:
      - ./data:/app/data
    env_file:
      - .env
